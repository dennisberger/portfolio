---
import Base from '@/layouts/Base.astro';
---

<Base title="404 — Dennis Berger" description="Page not found." noindex>
  <section class="lost page">
    <p class="lost__kicker">404</p>
    <h1 class="lost__title">But why?</h1>
    <p class="lost__body">
      This is where my notes would've helped. Try <a href="/">home</a> or <a href="/work/">case studies</a>. If it's my fault, <a href="/contact/">tell me</a>.
    </p>
    <div class="lost__actions">
      <a href="/" class="btn btn--primary">Home</a>
      <a href="/work/" class="btn btn--ghost">Case studies</a>
    </div>
  </section>
</Base>

<style>
  .lost {
    padding-block: var(--space-section-hero);
    max-width: 40rem;
    text-align: left;
  }
  .lost__kicker {
    font-family: var(--font-display);
    font-size: var(--type-outsize);
    font-style: italic;
    font-weight: 300;
    color: var(--color-fuchsia);
    line-height: 0.9;
    letter-spacing: var(--tracking-outsize);
    margin-block-end: var(--space-sm);
    margin-inline-start: -0.04em;
  }
  .lost__title {
    margin-block-end: var(--space-lg);
  }
  .lost__body {
    font-family: var(--font-body);
    font-size: var(--type-lg);
    line-height: var(--leading-relaxed);
    color: var(--color-ink-muted);
    margin-block-end: var(--space-xl);
  }
  .lost__actions { display: flex; gap: var(--space-md); margin-block-end: var(--space-xl); flex-wrap: wrap; }
  .lost__footnote {
    font-family: var(--font-body);
    font-size: var(--type-sm);
    color: var(--color-ink-muted);
    font-style: italic;
  }
</style>
